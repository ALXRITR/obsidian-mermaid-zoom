// Moved: the plugin source now lives in src/ (see src/main.ts and the
// zoom / icons / theme / pipeline / settings modules). This file is no longer
// part of the build (esbuild entry = src/main.ts, tsconfig include = src/**).
// Kept only as a tombstone; safe to delete.
export {};
